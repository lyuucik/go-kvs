apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-cloud-ingress
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: go-cloud-svc
                port:
                  number: 80
